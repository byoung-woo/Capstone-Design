# --- 컴파일 및 빌드 관련 ---
*.o
webserver
# (혹시 1_http 등 다른 폴더의 실행 파일명이 다르다면 여기에 추가)

# --- 보안 및 설정 파일 (절대 올리면 안 됨) ---
certs/
config.sh
*.key
*.crt
*.pem

# --- 데이터베이스 및 로그 ---
*.db
*.log
webserver_access.log
webserver_attack.log
nohup.out

# --- 기타 바이너리 및 불필요한 파일 ---
*.tar.gz
snort-2.9.20
.DS_Store
